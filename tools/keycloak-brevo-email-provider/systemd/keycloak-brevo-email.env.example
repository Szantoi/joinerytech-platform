# Install the real file as /etc/spaceos/keycloak-brevo-email.env.
# It must be owned by root:root and mode 0600. Do not put this key in a realm,
# keycloak.conf, a provider SPI option, a shell history, Git, or service logs.
KC_BREVO_API_KEY=replace-with-brevo-transactional-api-key
